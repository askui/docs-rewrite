---
title: Install the CLI
description: Install the AskUI CLI to run Projects headless, in CI, or across many devices.
---

import { Aside } from '@astrojs/starlight/components';

The AskUI CLI runs the same Projects you author in AskUI Desktop — without the
UI. Use it in CI, on a schedule, or to drive runs across many devices.

```bash
# scaffold — final install command TBD
askui --version
```

<Aside type="note">
Stub page. Once the CLI distribution is finalized this covers install per
platform, authentication, and `askui run`. See
[Running from the CLI](/using-askui-desktop/cli/) for the run workflow.
</Aside>
